node_modules
# Keep environment variables out of version control
.env

# SQLite database files
*.db
*.db-journal

generated/

# tsc build artifacts (outDir is set to ./dist).
# `npm run dev` uses tsx and needs none of these.
dist/
